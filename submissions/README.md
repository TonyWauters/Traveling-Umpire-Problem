# Solution submissions

Place each solution and its JSON metadata in `submissions/<contributor>/` using the shared stem `umpsN_q1_q2`. Do not put example `.txt` files here because every matching file is treated as a real submission.

See [`../CONTRIBUTING.md`](../CONTRIBUTING.md) for the schema, local commands, and automated pull-request workflow.
